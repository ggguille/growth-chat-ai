[project]
name = "growth-chat"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "backend",
    "evaluation",
    "knowledge-ingest",
    "shared/knowledge_base",
]
