# Environment (set to "production" to disable /docs and /redoc)
APP_ENV=development

# Widget authentication
ZGC_API_KEY=

# LLM
LLM_STREAM_TIMEOUT_MS=10000

# Database (Neon PostgreSQL)
DATABASE_URL=

# Slack
SLACK_WEBHOOK_URL=
SLACK_BOT_TOKEN=

# Handoff fallback email
HANDOFF_FALLBACK_EMAIL=

# SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
